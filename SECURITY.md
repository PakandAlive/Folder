# 安全规范

## 凭据管理

仓库不得包含真实 API Key、访问令牌、刷新令牌、密码、Cookie、私钥或生产环境配置。脚本需要凭据时，必须通过环境变量或部署平台的 Secret 管理功能注入；示例中只能使用明确的占位符。

一旦凭据进入 Git 提交，应立即视为已经泄露。正确处置顺序是：

1. 在服务提供方撤销或轮换凭据。
2. 检查异常访问、调用量、权限和账单。
3. 从当前代码中移除凭据。
4. 重写仍可访问的 Git 历史并通知仓库使用者重新同步。

历史重写不能清除他人的克隆、Fork、缓存或日志，因此不能替代凭据轮换。

## 本地与个人数据

不得提交真实用户名路径、邮箱列表、目标主机列表、审计结果、数据库、日志或包含身份信息的截图。相关文件应保留在本机，并由 `.gitignore` 排除。

## 授权边界

`scripts/security/` 下的工具只能用于本人拥有或已获得明确授权的系统。不得使用这些脚本扫描、尝试登录或干扰未授权目标。

## 报告问题

发现敏感信息时，不要在公开 Issue、提交信息或聊天记录中粘贴原值。报告应只包含文件路径、提交位置、凭据类型和脱敏后的识别信息。
