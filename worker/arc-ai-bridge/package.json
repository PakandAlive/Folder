{
  "name": "arc-ai-bridge-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: Arc Ask on Page AI bridge + LaunchDarkly SSE targeted rewrite",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "syntax": "node --check src/index.js && node --check src/http.js && node --check src/bridge.js && node --check src/sse.js && node --check src/launchdarkly.js"
  }
}
